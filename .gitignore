# Anchored: an unanchored "curator" also matches the cmd/curator/ directory,
# which silently excluded the whole command from git.
/curator
*.db
*.db-shm
*.db-wal
.env
.DS_Store
/web/node_modules/
/web/out/
.idea/
.vscode/
